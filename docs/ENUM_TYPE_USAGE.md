# Использование типа данных ENUM

## Описание

Тип данных `enum` позволяет создавать свойства с ограниченным набором значений, каждое из которых имеет своё описание.

## Создание свойства с типом enum

1. При создании или редактировании свойства выберите тип **Enum**
2. В поле **Parameters (JSON)** укажите JSON-объект со значениями и их описаниями

### Поддерживаемые форматы

Вы можете использовать любой из следующих форматов:

**Формат 1: JSON с числовыми ключами (строки)**
```json
{
  "enum_values": {
    "0": "Выключено",
    "1": "Включено",
    "2": "Ошибка"
  }
}
```

**Формат 2: Python dict с числовыми ключами (для поля Enum Values)**
```python
{
  0: "Выключено",
  1: "Включено",
  2: "Ошибка"
}
```

**Формат 3: JSON с текстовыми ключами**
```json
{
  "enum_values": {
    "off": "Выключено",
    "on": "Включено",
    "error": "Ошибка"
  }
}
```

> 💡 **Примечание:** 
> - Все форматы автоматически нормализуются - числовые ключи преобразуются в строки
> - Enum значения хранятся в отдельном поле `enum_values` для избежания конфликтов
> - В UI можно вводить значения как с `enum_values`, так и без (автоматически обернется)

### Пример

Создадим свойство `status` с типом `enum`.

**В поле "Enum Values (JSON)" вводим:**
```json
{
  "0": "Выключено",
  "1": "Включено",
  "2": "Ошибка",
  "3": "Обслуживание"
}
```

Или можно написать проще (Python dict формат):
```python
{
  0: "Выключено",
  1: "Включено",
  2: "Ошибка",
  3: "Обслуживание"
}
```

**Итоговая структура в params:**
```json
{
  "enum_values": {
    "0": "Выключено",
    "1": "Включено",
    "2": "Ошибка",
    "3": "Обслуживание"
  },
  "icon": "fas fa-power-off",
  "color": "#4CAF50",
  "default_value": "0"
}
```

## Использование в коде

### Установка значения

```python
# Установить значение
obj.setProperty("status", "1")
# или
obj.status = "1"
```

### Получение значения

```python
# Получить значение (ключ)
value = obj.getProperty("status")
# value = "1"

# Получить описание значения
text = obj.getProperty("status", data="text")
# text = "Включено"
```

### Через API

```python
# GET запрос для получения значения
response = requests.get(f"/api/objects/{object_name}/properties/{property_name}")
# response.json() = {
#     "value": "1",
#     "text": "Включено",
#     "params": {
#         "0": "Выключено",
#         "1": "Включено",
#         "2": "Ошибка",
#         "3": "Обслуживание"
#     }
# }
```

## Преимущества

1. **Валидация данных** - значения ограничены заданным списком
2. **Читаемость** - описания делают код более понятным
3. **Интернационализация** - можно легко изменить описания для разных языков
4. **API** - описания доступны через API и интерфейс

## Примеры использования

### Режимы работы устройства

```json
{
  "enum_values": {
    "auto": "Автоматический режим",
    "manual": "Ручной режим",
    "eco": "Экономичный режим",
    "turbo": "Турбо режим"
  }
}
```

### Статусы задачи

```json
{
  "enum_values": {
    "pending": "Ожидает выполнения",
    "in_progress": "Выполняется",
    "completed": "Завершена",
    "failed": "Ошибка",
    "cancelled": "Отменена"
  }
}
```

### Приоритет

```json
{
  "enum_values": {
    "1": "Низкий",
    "2": "Средний",
    "3": "Высокий",
    "4": "Критический"
  }
}
```

### Пример с "конфликтными" значениями

Теперь можно безопасно использовать системные названия как enum значения:

```json
{
  "enum_values": {
    "icon": "Иконка",
    "color": "Цвет",
    "min": "Минимум",
    "max": "Максимум"
  },
  "icon": "fas fa-palette",
  "color": "#FF5722"
}
```
