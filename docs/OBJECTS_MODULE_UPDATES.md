# Обновление модуля Objects - Расширенные валидации

## ✅ Что добавлено

В модуль **Objects** добавлена возможность задавать 4 новых типа валидации для свойств через веб-интерфейс:

### 1. **Step** (Дискретность) 
- Для типов: `int`, `float`
- Поле в UI: "Step (Discreteness)"
- Пример: яркость с шагом 10%, температура с шагом 0.5°C

### 2. **Allowed Values** (Разрешенные значения)
- Для всех типов кроме `enum`
- Поле в UI: "Allowed Values"
- Пример: приоритет [1, 2, 3, 5, 10], цвета ["red", "green", "blue"]

### 3. **Rate Limit** (Ограничение частоты)
- Для всех типов
- Поле в UI: "Rate Limit (seconds)"
- Пример: кнопка с защитой от дребезга (2 сек)

### 4. **Depends On** (Зависимости)
- Для всех типов
- Поле в UI: "Dependencies (depends_on)"
- Пример: мощность зависит от режима работы

---

## 📍 Где находится

**Путь в системе:**
1. Откройте модуль **Objects** в административной панели
2. Выберите класс или объект
3. Перейдите на вкладку **Properties**
4. Нажмите **Edit** на свойстве или создайте новое

---

## 🎨 Интерфейс

### Раздел "Validation Parameters"
Базовые параметры валидации (уже были):
- Min/Max значения (int, float)
- Decimals (float)
- **NEW: Step** - шаг дискретности (int, float)
- RegExp паттерн (str)
- Enum Values (enum)

### Раздел "Advanced Validation" (НОВЫЙ!)
Расширенные параметры валидации:
- **Allowed Values** - список разрешенных значений (JSON массив)
- **Rate Limit** - минимальный интервал между изменениями (секунды)
- **Dependencies** - зависимости от других свойств (JSON объект)

---

## 📝 Примеры заполнения через UI

### Пример 1: Яркость LED с шагом 10%

1. **Name:** `brightness`
2. **Type:** `int`
3. **Validation Parameters:**
   - Minimum Value: `0`
   - Maximum Value: `100`
   - Step (Discreteness): `10`
4. **Additional Parameters:**
   - Icon: `lightbulb`
   - Color: `#FFC107`

**Результат:** Можно установить только: 0, 10, 20, 30, ..., 100

---

### Пример 2: Приоритет с ограниченными значениями

1. **Name:** `priority`
2. **Type:** `int`
3. **Advanced Validation:**
   - Allowed Values: `[1, 2, 3, 5, 10]`

**Результат:** Можно установить только: 1, 2, 3, 5 или 10

---

### Пример 3: Кнопка с защитой от дребезга

1. **Name:** `button_pressed`
2. **Type:** `bool`
3. **Advanced Validation:**
   - Rate Limit (seconds): `2.0`

**Результат:** Можно нажать не чаще раза в 2 секунды

---

### Пример 4: Мощность зависит от режима

**Шаг 1 - Создать свойство mode:**
1. **Name:** `mode`
2. **Type:** `enum`
3. **Validation Parameters:**
   - Enum Values (JSON):
   ```json
   {
     "auto": "Автоматический",
     "manual": "Ручной",
     "off": "Выключен"
   }
   ```

**Шаг 2 - Создать свойство power с зависимостью:**
1. **Name:** `power`
2. **Type:** `int`
3. **Validation Parameters:**
   - Minimum Value: `0`
   - Maximum Value: `2000`
4. **Advanced Validation:**
   - Dependencies (depends_on):
   ```json
   {
     "property": "mode",
     "value": "manual",
     "condition": "equals",
     "error_message": "Мощность можно изменять только в ручном режиме"
   }
   ```

**Результат:** Свойство `power` можно изменить только если `mode` = "manual"

---

## 🔍 Формат JSON для зависимостей

### Простая зависимость:
```json
{
  "property": "enabled",
  "value": true,
  "condition": "equals"
}
```

### С пользовательским сообщением:
```json
{
  "property": "temperature",
  "value": 25.0,
  "condition": "greater_than",
  "error_message": "Температура должна быть выше 25°C"
}
```

### Множественные зависимости:
```json
[
  {
    "property": "enabled",
    "value": true,
    "condition": "equals"
  },
  {
    "property": "mode",
    "value": "manual",
    "condition": "equals"
  }
]
```

### Доступные условия:
- `equals` - равно
- `not_equals` - не равно
- `greater_than` - больше
- `less_than` - меньше
- `greater_or_equal` - больше или равно
- `less_or_equal` - меньше или равно
- `in` - входит в список
- `not_in` - не входит в список

---

## 📁 Обновленные файлы

### Frontend (UI)
- ✅ `plugins/Objects/templates/property.html` - добавлены поля для новых параметров

### Документация
- ✅ `plugins/Objects/VALIDATION_GUIDE.md` - полное руководство по использованию
- ✅ `docs/OBJECTS_MODULE_UPDATES.md` - этот файл
- ✅ `PARAMS_DOCUMENTATION.md` - обновлена техническая документация

### Backend (уже было реализовано ранее)
- ✅ `app/core/main/ObjectManager.py` - логика валидации
- ✅ `tests/test_advanced_validation.py` - тесты
- ✅ `examples/advanced_validation_examples.py` - примеры кода

---

## 🚀 Как использовать прямо сейчас

1. **Откройте административную панель**
   - Перейдите в модуль **Objects**

2. **Выберите объект или класс**
   - Нажмите на нужный объект/класс

3. **Откройте вкладку Properties**
   - Нажмите **Add** для создания или **Edit** для редактирования

4. **Заполните новые поля**
   - Прокрутите вниз до раздела "Advanced Validation"
   - Заполните нужные параметры
   - Нажмите **Submit**

5. **Проверьте работу**
   - Попробуйте изменить значение свойства
   - Система автоматически проверит все валидации

---

## 💡 Полезные советы

### Step (Дискретность)
✅ Используйте для:
- UI слайдеров и регуляторов
- IoT устройств с фиксированными шагами
- Примеры: яркость (10), температура (0.5), громкость (5)

### Allowed Values
✅ Используйте для:
- Ограниченного набора числовых значений
- Предустановленных значений
- Примеры: приоритеты [1,2,3,5], множители [0.5,1.0,1.5,2.0]

### Rate Limit
✅ Используйте для:
- Защиты кнопок от дребезга (1-3 сек)
- Ограничения частоты обновления датчиков (5-60 сек)
- Защиты от чрезмерной нагрузки

### Depends On
✅ Используйте для:
- Логической связи между свойствами
- Предотвращения некорректных состояний
- Условной доступности свойств

---

## 📚 Дополнительная документация

- **Руководство по валидациям:** `plugins/Objects/VALIDATION_GUIDE.md`
- **Техническая документация:** `PARAMS_DOCUMENTATION.md`
- **Примеры кода:** `examples/advanced_validation_examples.py`
- **Тесты:** `tests/test_advanced_validation.py`
- **Резюме реализации:** `docs/ADVANCED_VALIDATION_SUMMARY.md`

---

## ✨ Готово к использованию!

Все функции полностью реализованы и готовы к использованию через веб-интерфейс модуля Objects.
Начните создавать свойства с расширенными валидациями прямо сейчас!
